{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div> <!-- para cada erro vai printar -->
{{else}}
{{/each}}

<div class="card">
    <div class="card-body">
        <h4>Nova postagem</h4>
        <br>
        <form action="/admin/postagem/nova" method="POST">
            <label for="anoFabricacao">Ano de fabricação: </label>
            <input type="text" name="anoFabricacao" class="form-control">
            <label for="cor">Cor: </label>
            <input type="text" name="cor" class="form-control">
            <label for="preco">Preço: </label>
            <input type="text" name="preco" class="form-control">
            <label for="descricao">Descrição: </label>
            <textarea name="descricao" class="form-control"></textarea>
            <br>
            <label for="veiculo">Veiculo: </label>
            <select name="veiculo" class="custom-select">
                {{#each veiculo}}
                    <option value="{{_id}}">{{nome}}</option>
                {{else}}
                    <option value="0">Nenhum veiculo registrado</option>
                {{/each}}
               
            </select>
            <br>
            <br>
            <button class="btn btn-success" type="submit">Criar postagem</button>
        </form>
    </div>
</div>