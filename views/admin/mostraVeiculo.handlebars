<h2>Lista de veículos por marca: </h2>
<a href="/admin/cad"><button class="btn btn-success">Voltar</button></a>

{{#each veiculo}}
    <div class="card mt-4">
        <div class="card-body">
            <h4>{{nome}}</h4>
            <small>Marca: {{marca}}</small>
            <br>
        </div>
    </div>
{{else}}

<h4 class="mt-3">Nenhum veiculo cadastrado !</h4>
{{/each}}